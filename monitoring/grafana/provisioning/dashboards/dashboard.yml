apiVersion: 1
 
providers:
  - name: "Default"
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards    # Where Grafana looks for JSON files
      foldersFromFilesStructure: false